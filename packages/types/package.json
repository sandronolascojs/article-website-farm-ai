{
  "name": "@auto-articles/types",
  "version": "1.0.0",
  "description": "Types for the Mentor Glide AI project (schemas, etc.)",
  "main": "index.ts",
  "author": "Mentor Glide AI",
  "license": "MIT",
  "scripts": {
    "dev": "tsx watch index.ts",
    "typecheck": "tsc --noEmit",
    "format": "prettier --write --ignore-path .gitignore --config ../config/.prettierrc .",
    "lint": "eslint . --ext .js,.ts,.tsx"
  },
  "dependencies": {
    "zod": "catalog:",
    "tsx": "catalog:",
    "@auto-articles/utils": "workspace:*",
    "@auto-articles/config": "workspace:*"
  },
  "devDependencies": {
    "typescript": "catalog:",
    "prettier": "catalog:",
    "eslint": "catalog:"
  }
}
